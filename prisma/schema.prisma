model User {

  id       String @id @default(uuid())
  userName String @unique

  @@map("users")
}

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
