type Post {
  id: String
  title: String
  body: String
  userId: String
  indexRef: Int
  createdAt: String
  comments: [Comment]
}

type PostForm {
  id: String
  title: String
  body: String
  userId: String
  indexRef: Int
}
